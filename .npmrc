# Electron 二进制下载镜像（默认走 GitHub，国内网络会失败导致 "Electron uninstall" 报错）
# 用环境变量而非 npm config key（npm 11+ 不再认 electron_mirror 等非标准 key）
# @electron/get 读 ELECTRON_MIRROR，electron-builder 读 ELECTRON_BUILDER_BINARIES_MIRROR
